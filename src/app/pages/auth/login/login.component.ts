import { Component, OnInit} from '@angular/core';
import { NbLoginComponent } from '@nebular/auth';

@Component({
  selector: 'slab-stonks-login',
  templateUrl: './login.component.html',
})

export class SlabLoginComponent extends NbLoginComponent {
}
